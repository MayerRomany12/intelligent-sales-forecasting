# ========================================================
# Olist Sales Forecasting System Environment Configuration
# ========================================================

# --- BACKEND (FastAPI) Configuration ---
PORT=8000
HOST=0.0.0.0
MODEL_PATH=Models/best_xgboost_model.json
CORS_ORIGINS=http://localhost:3000,http://localhost:8501,http://127.0.0.1:3000,http://127.0.0.1:8501

# --- STREAMLIT DASHBOARD Configuration ---
# Set API_URL to target the FastAPI backend (use service name in docker networks e.g. http://backend:8000)
API_URL=http://localhost:8000

# --- FRONTEND (React) Configuration ---
# API endpoint targeted by the React client-side build
VITE_API_URL=http://localhost:8000
